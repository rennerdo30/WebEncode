version: "2"
sql:
  - schema: "pkg/db/migrations"
    queries: "pkg/db/queries"
    engine: "postgresql"
    gen:
      go:
        package: "store"
        out: "pkg/db/store"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
