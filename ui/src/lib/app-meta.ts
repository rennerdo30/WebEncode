/** Values shown in the app shell (sidebar footer, page footer, metadata). */
export const APP_NAME = "WebEncode";
export const APP_VERSION = "v1.0.0";
export const APP_LICENSE = "MIT License";

export const REPO_URL = "https://github.com/rennerdo30/WebEncode";

/*
  The dashboard has no in-app docs routes, so the footer links to the docs that
  actually exist in the repository rather than to dead internal paths.
*/
export const DOCS_URL = `${REPO_URL}/tree/main/docs`;
export const API_DOCS_URL = `${REPO_URL}/blob/main/docs/API_REFERENCE.md`;
